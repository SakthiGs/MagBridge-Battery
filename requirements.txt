# Core dependencies for MagBridge-Battery
# Tested with Python 3.10–3.12

# Data formats
pandas>=2.0
pyarrow>=15.0
numpy>=1.24

# Classical ML benchmarks (run_benchmark.py)
scikit-learn>=1.3

# Deep learning benchmarks (run_dl_bench_lean.py)
torch>=2.0

# Bridge package — Pydantic v2 for schema validation
pydantic>=2.0
pyyaml>=6.0

# Quantum reservoir embedding (MagBridge-Embed) — only needed if
# regenerating the dataset from scratch
pennylane>=0.40

# Tests
pytest>=7.0
